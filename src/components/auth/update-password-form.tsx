"use client";
import Link from "next/link"; import { useActionState } from "react"; import { Alert,AlertDescription } from "@/components/ui/alert"; import { Button } from "@/components/ui/button"; import { Field,FieldLabel } from "@/components/ui/field"; import { Input } from "@/components/ui/input"; import { updatePassword,type PasswordResetState } from "@/features/auth/actions"; import type { Locale } from "@/i18n/config"; import type { Dictionary } from "@/i18n/types";
export function UpdatePasswordForm({locale,dictionary}:{locale:Locale;dictionary:Dictionary}){const[state,action,pending]=useActionState(updatePassword,{} as PasswordResetState);if(state.success)return <Alert><AlertDescription>Password updated. <Link className="font-semibold underline" href={`/${locale}/login`}>{dictionary.common.signIn}</Link></AlertDescription></Alert>;return <form action={action} className="space-y-5">{state.error?<Alert variant="destructive"><AlertDescription>Use at least 8 characters and try again.</AlertDescription></Alert>:null}<Field><FieldLabel htmlFor="password">New {dictionary.auth.password}</FieldLabel><Input id="password" name="password" type="password" minLength={8} autoComplete="new-password" required/></Field><Button className="w-full" disabled={pending}>Update password</Button></form>}
