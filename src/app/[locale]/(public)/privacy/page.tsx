import { notFound } from "next/navigation"; import { PublicPageHero } from "@/components/public-site/public-page-hero"; import { getPublicCopy } from "@/content/public-pages"; import { isLocale } from "@/i18n/config";
export default async function Privacy({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const copy=getPublicCopy(locale);const p=copy.policy.privacy;return <main id="main-content"><PublicPageHero eyebrow={p[0]} title={p[0]} description={p[1]}/><div className="mx-auto max-w-3xl px-4 py-16 text-muted-foreground sm:px-6"><p className="leading-8">{p[1]}</p></div></main>}
