import Link from "next/link"; import { notFound } from "next/navigation"; import { ForgotPasswordForm } from "@/components/auth/forgot-password-form"; import { AcademyBrand } from "@/components/brand/academy-brand"; import { Button } from "@/components/ui/button"; import { Card,CardContent,CardDescription,CardHeader,CardTitle } from "@/components/ui/card"; import { isLocale } from "@/i18n/config"; import { getDictionary } from "@/i18n/dictionaries";
export default async function ForgotPassword({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const dictionary=await getDictionary(locale);return <div className="flex w-full max-w-md flex-col gap-7"><AcademyBrand href={`/${locale}`} name={dictionary.common.brandName}/><Card><CardHeader><CardTitle className="text-3xl">{dictionary.auth.resetTitle}</CardTitle><CardDescription>{dictionary.auth.resetDescription}</CardDescription></CardHeader><CardContent><ForgotPasswordForm locale={locale} dictionary={dictionary}/></CardContent></Card><Button variant="ghost" asChild><Link href={`/${locale}/login`}>{dictionary.common.signIn}</Link></Button></div>}
