import { notFound } from "next/navigation"; import { AcademyBrand } from "@/components/brand/academy-brand"; import { UpdatePasswordForm } from "@/components/auth/update-password-form"; import { Card,CardContent,CardDescription,CardHeader,CardTitle } from "@/components/ui/card"; import { isLocale } from "@/i18n/config"; import { getDictionary } from "@/i18n/dictionaries";
export default async function UpdatePasswordPage({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const dictionary=await getDictionary(locale);return <div className="flex w-full max-w-md flex-col gap-7"><AcademyBrand href={`/${locale}`} name={dictionary.common.brandName}/><Card><CardHeader><CardTitle className="text-3xl">Choose a new password</CardTitle><CardDescription>Use at least 8 characters.</CardDescription></CardHeader><CardContent><UpdatePasswordForm locale={locale} dictionary={dictionary}/></CardContent></Card></div>}
